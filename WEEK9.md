# Week 9：LLM Provider 边界与 Token-aware 上下文

> 项目：`ai-chat-backend-v2`  
> 来源：`other/LearningPlan.md` 第 9～12 周“LLM 原理与 API 工程”  
> 本周定位：先建立稳定的模型调用边界，再让上下文选择从“固定消息条数”升级为“按 token 预算决策”。

## 启动前置条件

- [ ] 将 Week 8 的工程改动和文档整理为可审查的提交
- [ ] 确认提交后工作树没有意外修改，再开始 Week 9 的代码实现
- [ ] 保留现有 129 个测试、质量门禁和 fail-closed 测试隔离能力

Week 9 不在未提交的 Week 8 改动上继续堆叠实现。周计划文件可以先建立，但 Day 1 的代码要在前置条件满足后开始。

## 本周架构问题

当前 Chat 链路直接依赖具体模型接口，而且历史消息仍主要按固定条数截断。这会产生两个问题：

1. 业务层理解供应商的请求格式、响应格式和异常，切换模型或测试时改动范围过大。
2. 消息条数不能代表上下文大小；少量长消息仍可能超过模型窗口，大量短消息又可能被过早丢弃。

本周将调用链调整为：

```text
Chat Application Service
  → Provider-neutral contract
  → Provider Adapter
  → LLM HTTP API

Chat Application Service
  → Token Counter
  → Context Budget Policy
  → selected messages
```

## 本周目标

- [ ] 定义与供应商无关的普通响应和流式响应契约
- [ ] 将具体供应商的鉴权、HTTP 格式、SSE 解析和异常映射限制在 adapter 内
- [ ] 让真实 adapter 与测试 fake/mock 遵守同一份契约
- [ ] 建立 tokenizer 抽象，明确 token 计数只是模型相关估算而不是字符计数
- [ ] 定义上下文预算：模型窗口需要同时容纳系统提示、历史、新输入和输出预留
- [ ] 用 token-aware 策略替代固定 20 条历史消息，同时保持消息顺序与用户隔离
- [ ] 建立第一批确定性的 LLM 调用与上下文回归样例
- [ ] 保证测试与 CI 不请求真实模型，也不依赖本地开发密钥

## 每日主题与边界

### Day 1：调用链审计与 Provider Contract

识别当前业务层和具体供应商之间的耦合，划定 provider contract、配置边界、普通/流式语义和统一错误分类。当天先把不变量与接口讲清楚，不急着加入多个真实供应商。

### Day 2：Provider Adapter 落地

把现有模型调用迁入具体 adapter，通过依赖注入让 Chat 业务只依赖抽象；用 fake/mock 验证普通与流式路径遵守同一契约。

### Day 3：Tokenizer 与上下文预算模型

学习 token、模型上下文窗口和输出预留，定义 token counter 与预算计算规则；先用测试固定预算边界，不立刻改动全部 Chat 行为。

### Day 4：Token-aware 上下文选择

把预算策略接入 Chat 链路，替代固定消息条数；验证超长输入、历史裁剪、顺序、最近消息和多用户隔离等正确性边界。

### Day 5：回归基线与架构复盘

建立 Week 9 第一批确定性回归样例，运行完整质量门禁，并用数据流、失败路径、扩展方式和方案代价解释本周架构。40～50 个完整回归用例是第 9～12 周阶段目标，不在本周为了凑数一次写完。

> 每天的知识、实现任务、测试和验收细则只在当天开始时补充，并依据前一天的实际结果动态调整。

## 重要内容

- Provider contract 是业务与外部模型之间的防腐层，不只是把 HTTP 请求换个文件存放。
- 依赖倒置：Chat 业务依赖能力契约，具体 adapter 依赖并实现该契约。
- 普通响应与流式响应必须明确完成、上游失败、格式错误、超时和调用方取消时的行为。
- 供应商错误要在边界处归一化，但不能吞掉足以定位问题的错误类型和关联信息。
- token 数量依赖模型及 tokenizer；字符数、字数和消息条数都不能精确代替 token。
- 上下文预算的核心不变量：输入预算加输出预留不得超过所选模型的上下文窗口。
- 历史裁剪必须保持时间顺序、会话归属和用户隔离，不能为了填满预算破坏语义。
- 测试安全由依赖注入和配置护栏保证：测试默认 fake/mock provider，真实调用必须显式配置并与自动化测试分离。

## 本周暂不加入

- RAG、Embedding、向量数据库、Agent、MCP
- 同时接入多个真实模型供应商
- 自动 fallback、熔断、复杂重试、成本路由和模型动态选择
- tool calling 与完整的结构化输出体系
- prompt 管理平台、在线评测平台和生产级 token 成本核算
- 为达到数量而一次性补齐 40～50 个低价值测试样例

这些内容属于第 9～12 周后续阶段；本周先保证边界正确、预算可解释、行为可测试。

## 周级验收标准

- [ ] Chat 业务代码中不再出现具体供应商的 HTTP/SSE 数据格式
- [ ] 更换 fake provider 不需要修改 Chat 业务逻辑
- [ ] 普通与流式调用的成功和主要失败语义有契约测试
- [ ] 上下文选择依据显式 token 预算，而不是固定取 20 条
- [ ] 预算边界、超长输入和历史裁剪均有确定性测试
- [ ] 完整测试、Ruff、Pyright、pre-commit 与覆盖率门禁继续通过
- [ ] 能独立画出调用数据流，并解释 provider 边界和 token 预算的主要取舍

## Week 10 衔接方向

根据 Week 9 验收结果，再确定 Week 10 的每日边界。优先候选包括结构化输出、JSON Schema、解析失败策略和回归集扩展；不在本周提前锁定具体实现细则。
