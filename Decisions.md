# 设计决策记录（Decisions）

记录本项目有取舍的设计决定。原则：**每个决定写清楚背景、选择、理由和代价**，避免以后“不知道为什么当初这么写”。

---

## D-001：流式回复的保存策略

- 日期：2026-08-05
- 背景：`/chat/stream` 在流式输出过程中，客户端可能中途断开，LLM 也可能中途报错；需要决定“已收到的半截内容要不要落库、怎么落库”。
- 决定：
  - `generate()` 的 `finally` 总是尝试把已收到的内容保存为 assistant 消息；
  - LLM 报错时，在已收到的内容后追加 `[流式响应中断：...]` 再保存；
  - 客户端断开（非 LLM 报错）时，不加中断标记，原样保存半截内容。
  - `messages.is_complete` 明确记录完整性：上游流自然结束为 `true`，LLM
    中断或客户端提前关闭为 `false`；所有消息写入者必须显式声明该字段。
- 理由：尽量保留用户已经看到的内容；学习阶段优先简单实现，先保证“消息不会丢”。
- 当前状态（2026-08-12）：**已完成**。迁移已回填历史消息为完整；正常结束、
  LLM 中断和客户端 `aclose()` 三种路径均有自动测试。

## D-002：短事务 2（保存 assistant 消息）失败的兜底策略

- 日期：2026-08-05
- 背景：客户端已经收到流式内容、HTTP 状态码已是 200，此时落库失败无法再改状态码，会出现“用户看到回答但历史里没有”的不一致。
- 决定（目标）：
  - 保存失败**不得破坏已经发出的响应**：捕获异常、记录日志（后续可加重试队列），而不是让异常从生成器抛出、把流弄断；
  - 查询历史时优先以数据库为准，缺失记录通过日志/重试补偿。
- 当前状态（2026-08-12）：**本周兜底已完成**。
  - `save_message()` 遇到写入或提交失败时先 rollback，再重新抛出；真实
    PostgreSQL 约束失败测试证明同一个 session 随后可以继续查询；
  - 只有已经发出响应的流式 assistant 最终保存会在上层捕获：记录脱敏元数据，
    不重新抛出，不破坏客户端已经收到的内容；
  - SQLAlchemy engine 启用 `hide_parameters=True`，日志不记录消息正文、请求体、
    token 或数据库异常文本。
- 后续待办：补偿队列与自动重试留到引入 Worker 时实现。

---

## D-003：conversations 列表索引与排序方向

- 日期：2026-08-06
- 背景：Week 7 引入用户归属后，会话列表变为按 `user_id` 过滤；组合索引的最左前缀必须匹配过滤条件，第二列应跟随列表的实际排序字段。
- 决定：
  - `idx_conversations_user_id_created_at` 以 `user_id` 为最左前缀，第二列为会话列表的排序字段；
  - 目标排序按 `created_at`，即索引方向为 `(user_id, created_at)`；
  - 后期若支持按 `created_at` 或修改时间（如新增 `updated_at`）可选排序，再按实际查询补充/调整索引。
- 理由：组合索引最左前缀原则；第二列与排序方向一致可以避免“过滤走索引、排序额外开销”的情况。
- 已落地（2026-08-09）：模型与迁移中的索引已统一为 `(user_id, created_at)`（`idx_conversations_user_id_created_at`），与决策目标一致。

---

## D-004: conversation 添加 user_id 迁移策略

- 日期: 2026-08-09
- 背景: 添加 users 表后添加 user_id 作为 conversation 的外键
- 决定：
  - 迁移内做 backfill：`user_id` 先以可空列添加，已有数据统一归属给一个默认用户，保证数据不丢；
  - 本次迁移**不**把 `user_id` 设为 NOT NULL；NOT NULL 约束推迟到 Day 2 与“按当前用户过滤”的归属逻辑同批落地。
- 理由：如果结构约束先于代码写路径落地，会进入“数据库拒绝 NULL、代码又没传 user_id”的中间态，接口直接 500，测试全红。
- 当前状态（2026-08-12）：Day 2 已完成归属过滤，并通过独立后续迁移将
  `user_id` 设为 `NOT NULL`。

---

## D-005：会话列表缓存策略

- 日期：2026-08-17
- 背景：会话列表通常是“高频读、低频写”的数据，适合缓存。
- 选择：`cache-aside`，由应用程序负责先查缓存、缓存未命中再查数据库、写数据库后再让缓存失效；按用户分键，TTL 为 60 秒。
- 读流程：查 Redis → 命中返回 → 未命中查 PostgreSQL → 回填。
- 写流程：提交 PostgreSQL → 删除缓存。
- 故障策略：
  - 应用启动和测试环境 Redis 不可用时 fail-closed；
  - 运行期间缓存读写失败时 fail-open，回退数据库。
- 代价：短暂不一致、缓存击穿风险、代码复杂度增加。
- 安全：缓存键包含 `user_id`，缓存内容不包含密码、token 等敏感字段。

---

## D-006：JWT 访问令牌策略

- 日期：2026-08-17
- 背景：受保护接口需要识别当前用户，同时避免服务端为每个登录会话保存状态。
- 决定：
  - 访问令牌固定使用 HS256 签名；
  - payload 必须包含表示用户 ID 的 `sub` 和过期时间 `exp`；
  - `JWT_SECRET` 只从环境变量读取，并在启动时执行 fail-closed 校验；
  - payload 不存放密码、token、个人隐私等敏感信息，因为 JWT 经过签名但没有加密。
- 理由：当前单体服务使用 HS256 实现简单，`sub` 与 `exp` 足以完成身份识别和有效期限制。
- 代价：已签发的访问令牌无法主动撤销；后续可根据需求增加短期 Access Token、Refresh Token 轮换或 token 黑名单。

---

## D-007：资源归属与越权响应策略

- 日期：2026-08-17
- 背景：多用户环境中必须防止用户通过猜测会话 ID 访问他人的会话和消息。
- 决定：
  - `user_id` 只来自认证后的当前用户，不接受客户端自行指定；
  - Repository 查询资源时必须同时按资源 ID 和 `user_id` 过滤；
  - 访问他人资源与资源不存在统一返回 404，而不是返回 403。
- 理由：统一返回 404 可以避免向请求者泄露目标资源是否存在，并从查询层阻断 IDOR 越权访问。
- 代价：客户端无法区分“资源不存在”和“资源属于其他用户”；新增资源查询时必须持续遵守归属过滤规则并用测试防止遗漏。

---

## D-008：重试任务的投递与幂等策略

- 日期：2026-08-17
- 背景：流式响应中，客户端可能已经收到 assistant 回复，但最终的 `save_message()` 因数据库故障而失败。此时需要异步重试保存，同时保证同一任务被重复投递时不会产生重复消息。
- 决定：
  - 采用 at-least-once 投递，不追求消息队列层面的 exactly-once；
  - 任务首次创建时生成稳定的 `idempotency_key`，同一任务的每次重试必须沿用该 key；
  - `messages.idempotency_key` 可为 `NULL`，但非空值由数据库唯一约束或唯一索引保证只写入一次；
  - Consumer 固定按“校验任务 → 原子幂等写入 → 数据库 commit 成功 → ACK”的顺序执行；
  - 如果任务重复投递且数据库中已有相同 `idempotency_key`，不再创建第二条消息，但仍 ACK 该任务。
- 理由：如果先 `SELECT` 检查再 `INSERT`，两个 Worker 可能同时查到“不存在”并分别插入，形成并发竞态。数据库唯一约束在最终写入点原子地裁决重复任务，能在多 Worker 场景下保证幂等。
- 代价：
  - 队列可能重复投递，Consumer 必须将重复任务视为正常情况；
  - 数据库需要增加 `idempotency_key` 字段和唯一索引；
  - 该策略只保证数据库消息不重复，不能自动保证所有外部副作用都 exactly-once；
  - 后续仍需要补充重试次数、死信队列和监控告警。
- 失败场景：
  - 数据库 commit 前失败：不 ACK，任务会在之后重新投递；
  - 数据库 commit 成功、ACK 前 Worker 崩溃：任务会重复投递；重复写入被唯一约束拦截，随后 ACK；
  - 收到重复任务：相同 `idempotency_key` 不产生第二条消息；
  - 任务格式错误：不得进入数据库写路径，应进入死信队列；
  - 重试次数耗尽：后续进入死信队列，等待人工处理或补偿。

---

更新约定：每次做出有取舍的设计决定，在这里追加一条，并同步更新对应代码或测试，避免“决策文件说一套、代码做一套”。
