## 需求与 v1 问题：
我们要做的是一款可以和 AI 进行聊天的软件，包括流式聊天的部分都已经在 V1 版本实现了，但当时仅仅是一个能用的程序，更没有完整的异步和并发能力，进入 v2 版本，主要就是提升程序的稳定性，让程序从能用提升到不易出错且有一定的兜底及异步、较为完善的错误处理能力，并且引入了多用户管理系统

## 总体架构：
主体由 FastAPI+PostgreSQL 组成，使用 Alembic 进行数据库的版本管理，ORM 映射类作为数据库和程序的桥梁，使用 Redis 实现 cache-aside 旁路缓存架构缓存常用的会话信息以及 Redis Stream + Worker 处理可能出现的信息保存失败的重试队列
开发分成了三层，api、service、repository，api 是功能接口，service 是真正的业务逻辑，repository 是数据库操作，互相没有依赖纠缠
API 与 Worker 因入口、生命周期和故障边界等的不同使用不同的进程，但都依赖相同的环境，Migrate 数据库迁移作为一次性服务在 API 与 Worker 进程之前进行，防止两个进程因并发同时进行数据库迁移产生冲突

## 数据与事务：
repository 层管理PostgreSQL 的元事务，事务的终止和边界由 service 层管理，在请求网络的时候归还事务防止空载
在 v2 版本中还加入了用户及相应权限约束系统，用户只能访问该用户自己的会话
Alembic 管理数据库版本并上传 git，历史可溯源

## 认证与缓存：
用 JWT 进行身份认证与签名，用 RBAC 进行角色的访问控制，只有admin可以查询访问所有用户的接口，当前用户越权访问其他用户的会话会返回 404 防止 IDOR 攻击
缓存采用了应用程序自己管理内存而非数据库和缓存自动同步的 cache-aside 旁路缓存架构，因为该程序访问会话是最常用的操作，查询频率高、不经常修改且数据量不大
在创建新会话时会主动进行缓存失效，缓存失效在事务结束之后，也就是会话成功创建以后，防止并发时出现缓存删除、在数据库还没有成功写入时旧的数据库写进了新缓存导致缓存与数据库实际内容不一致的问题
缓存在程序运行与测试中采用了 fail-open / fail-close 故障策略设计，在程序启动的时候采用 fail-close，如果 Redis 启动失败就终止程序的启动，在程序运行的过程中采用 fail-open，如果 Redis 无响应就降级为直接访问数据库，保证程序的正常运行，如果数据库也没有正常运行将会记录日志概要，而测试的过程中为了能检测出缓存问题，不能进行降级，只能采取 fail-close 的策略，目前还给数据空中不存在的数据设定为空缓存防止缓存穿透问题
目前还没有解决的缓存问题：
- 为缓存 TTL 设置随机抖动防止大量常用缓存失效时产生的缓存雪崩问题
- 为会话列表查询添加分布式锁，缓存击穿问题，虽然目前风险较低

## 流式可靠性：
流式过程可能会出现网络连接突然终止，或者用户收到信息但是信息保存失败的情况，前者抓取网络连接的错误，在连接终止时将错误信息插入并将所有消息块进行拼接正常进行保存。如果信息保存失败，就将信息放入 Redis Stream 队列中并进入 pending 状态，Redis 自动分配进入 相应群组，Worder 进程对这些信息进行保存，定期 reclaim 再次获取没有成功保存的信息，直到信息保存成功发送 ACK 结束对该信息的保存，程序设计寻找 at-least-once 的原则，并为了数据库的幂等性为信息添加了 UUID key 来唯一标记，在 insert 时进行去重

## 可交付性：
目前项目已经实现 docker compose 对数据库缓存、迁移，API 服务后台 Worker 多进程的同一编排，接入 GitHub action 作为 CI 保证其在干净环境中的可运行性，测试隔离由代码框架保证避免人为对使用数据造成影响，目前的测试覆盖率达到了 91%，覆盖了大部分的异常分支，且经过了 50 用户压测
